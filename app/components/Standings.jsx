var React = require('react');

var Standings = (props) => {
  return (
    <div>
      <h1>Standings Page</h1>
      <p>To do:</p>
      <ul>
        <li>Get standings data from API</li>
        <li>Build standings page</li>
      </ul>
    </div>
  );
}

module.exports = Standings;
